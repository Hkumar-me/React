import React from 'react';
import ReactDOM from 'react-dom/client';

import Myname from './test';
const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <Myname/>
    <h1>Harsh is harsh</h1>
  </React.StrictMode>
);